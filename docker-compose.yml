services:
  web:
    build: .
    ports:
      - "8010:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - ALLOWED_HOSTS=localhost,127.0.0.1
      - DATABASE_PATH=/data/db.sqlite3
      - MEDIA_ROOT=/app/media
      # Optional: Auto-create superuser (set in .env file)
      - DJANGO_SUPERUSER_USERNAME=${DJANGO_SUPERUSER_USERNAME:-}
      - DJANGO_SUPERUSER_PASSWORD=${DJANGO_SUPERUSER_PASSWORD:-}
      - DJANGO_SUPERUSER_EMAIL=${DJANGO_SUPERUSER_EMAIL:-}
      # - BACKUP_CRON_ENABLED=true  # Enable scheduled database backups
    volumes:
      - db_data:/data
      - media_data:/app/media
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  worker:
    build: .
    command: python manage.py db_worker --queue-name "*"
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      - ALLOWED_HOSTS=localhost,127.0.0.1
      - DATABASE_PATH=/data/db.sqlite3
    volumes:
      - db_data:/data
    env_file:
      - .env
    depends_on:
      web:
        condition: service_healthy
    restart: unless-stopped

# PostgreSQL service (optional - uncomment to use instead of SQLite)
# See /help/database-postgresql/ for setup guide
#
#  db:
#    image: postgres:16-alpine
#    environment:
#      POSTGRES_DB: smallstack
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
#    volumes:
#      - postgres_data:/var/lib/postgresql/data
#    ports:
#      - "5432:5432"
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U postgres"]
#      interval: 5s
#      timeout: 5s
#      retries: 5
#
# To use PostgreSQL:
# 1. Uncomment the db service above
# 2. Add to web and worker environment:
#    - DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD:-postgres}@db:5432/smallstack
# 3. Add depends_on to web service:
#    depends_on:
#      db:
#        condition: service_healthy
# 4. Comment out or remove DATABASE_PATH lines
# 5. Add postgres_data to volumes below

volumes:
  db_data:
    driver: local
  media_data:
    driver: local
  # postgres_data:
  #   driver: local
