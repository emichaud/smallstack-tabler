# Django SmallStack - Environment Configuration
# Copy this file to .env and update the values

# ===========================================
# REQUIRED SETTINGS
# ===========================================

# Django secret key (optional for development)
# In development, a random key is auto-generated if not set.
# In Docker/production, docker-entrypoint.sh auto-generates and persists
# a key to /app/data/.secret_key on first deploy.
# You can still set one explicitly here to override auto-generation.
# SECRET_KEY=your-explicit-key-here

# Settings module
# Use "config.settings.development" for local development
# Use "config.settings.production" for Docker/production
DJANGO_SETTINGS_MODULE=config.settings.development

# Allowed hosts (comma-separated, required for production)
ALLOWED_HOSTS=localhost,127.0.0.1

# ===========================================
# AUTO-CREATE SUPERUSER (Docker)
# ===========================================
# Set these to automatically create an admin user on first Docker startup

# DJANGO_SUPERUSER_USERNAME=admin
# DJANGO_SUPERUSER_PASSWORD=your-secure-password
# DJANGO_SUPERUSER_EMAIL=admin@example.com

# ===========================================
# LOCAL DEVELOPMENT SUPERUSER
# ===========================================
# Used by "make superuser" or "make setup" for local development

DEV_SUPERUSER_USERNAME=admin
DEV_SUPERUSER_PASSWORD=admin

# ===========================================
# HTTPS SETTINGS (Production)
# ===========================================
# Enable these when using HTTPS (behind a reverse proxy)

SECURE_SSL_REDIRECT=false
SESSION_COOKIE_SECURE=false
CSRF_COOKIE_SECURE=false

# ===========================================
# TIMEZONE
# ===========================================
# System-wide default timezone for displaying dates/times.
# Users can override this in their profile settings.
# Uses IANA timezone names: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# TIME_ZONE=America/New_York

# For Docker: also set TZ so cron and system commands use the same timezone
# TZ=America/New_York

# ===========================================
# AUTH FEATURE FLAGS (Optional)
# ===========================================
# Control visibility of login/signup UI

# Set False to hide Login/Sign Up from topbar
# SMALLSTACK_LOGIN_ENABLED=True
# Set False to hide Sign Up and 404 the URL
# SMALLSTACK_SIGNUP_ENABLED=True

# ===========================================
# SCHEDULED TASKS
# ===========================================
# In Docker, supercronic starts automatically and runs:
#   - Heartbeat check — every minute
#   - Activity log pruning — every 15 minutes
#   - Database backup — daily at 2 AM (if BACKUP_CRON_ENABLED=true)
#
# These settings tune the scheduled task behavior.
# Defaults are shown — uncomment only to override.

# Set false to disable daily backups
BACKUP_CRON_ENABLED=true
# Days of raw heartbeat records to keep
# HEARTBEAT_RETENTION_DAYS=7
# Max activity log rows before pruning
# ACTIVITY_MAX_ROWS=10000

# ===========================================
# CORS (Frontend API Access)
# ===========================================
# Comma-separated origins allowed to call the REST API.
# Required when a frontend (React, Vue, etc.) runs on a different port/domain.
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# In development (DEBUG=True), if no origins are set, SmallStack auto-allows
# any localhost/127.0.0.1 origin — no config needed for local dev.

# ===========================================
# REST API Settings
# ===========================================
# Enable the /api/auth/register/ endpoint (requires auth-level token)
# SMALLSTACK_API_REGISTER_ENABLED=False

# Login token expiry (hours). Caps the expires_hours parameter on login/refresh.
# SMALLSTACK_LOGIN_TOKEN_EXPIRY_HOURS=24
# SMALLSTACK_LOGIN_TOKEN_MAX_HOURS=168

# ===========================================
# EMAIL SETTINGS (Optional)
# ===========================================
# Configure for password reset emails

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
# EMAIL_USE_TLS=true
